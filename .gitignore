# Это репозиторий приложения, а не репозиторий перевода.
# Реальный контент перевода живёт отдельно и в корень сюда попадать не должен.
# (Папка sample/ с тестовыми данными — исключение, она коммитится.)
/source/
/translation/
/variants/

# Системный мусор / редакторы
.DS_Store
Thumbs.db
*~
.idea/
.vscode/

# Секреты — на всякий случай, никогда не коммитим
*.key
.env
.env.*
