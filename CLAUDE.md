# CLAUDE.md — «Вычитка» (сверка перевода)

Инструмент для вычитки перевода: рядом показывает арабский оригинал, основной
русский перевод (редактируемый) и произвольное число справочных вариантов
(в т.ч. от других ИИ). Данные — `.md`-файлы на диске в репозитории перевода,
доступ через File System Access API. Запускается в Chrome/Edge на десктопе
(Linux и Windows).

## Как мы работаем

- Идеи, решения и тексты ТЗ приходят извне как краткие брифы. **Клудод владеет
  всем кодом и структурой репозитория.** Реализуй брифы, не отклоняясь от
  ограничений ниже.
- Если бриф противоречит этим правилам — останови и спроси, не «чини молча».

## Жёсткие ограничения (не нарушать без явного разрешения)

- Ванильные **HTML / CSS / JS**. Никаких фреймворков, никаких сборщиков,
  никакого шага build. Приложение — **один файл** `vychitka.html`.
- **Никаких внешних зависимостей**: ни CDN, ни npm, ни подгружаемых шрифтов.
  Только системные шрифты. Ядро должно работать офлайн.
- Распространяется как статика по HTTPS (Netlify, авто-деплой из репозитория
  приложения); `file://` остаётся рабочим запасным вариантом. Это чистая статика —
  под хостинг в коде ничего менять не нужно.
- Локальный режим — только десктопные Chrome/Edge (File System Access API).
  При отсутствии API локальный режим прячется, но приложение не отключается:
  режим GitHub работает в любом браузере, включая мобильные.

## Рассчитано на нескольких пользователей

Приложение раздаётся другим людям, не только автору:

- Ничего пользовательского не хардкодить. Все настройки (ключ API, раскладка
  слотов, последняя папка) — в `localStorage`/IndexedDB браузера конкретного
  пользователя, **не** в репозитории.
- В репозитории перевода — только контент (`source/`, `translation/`,
  `variants/`) и сам файл приложения. Никаких секретов и личных настроек.
- Интерфейс и сообщения рассчитаны на человека, который видит инструмент впервые.

## Модель данных

Каждый блок имеет **метку** (`метка`) — свободный текст, стабильный идентификатор:
`390-393`, `Глава 1`, `фатава 7`. Метка — это имя файлов блока и основа
спаривания и сортировки. У блока также есть **заголовок** — человекочитаемое
название, которое хранится **внутри** перевода (во фронтматтере), а НЕ в имени
файла.

Три папки в корне репозитория:

- `source/` — арабский оригинал, один файл на блок. Имя = метка: `390-393.md`,
  `Глава 1.md`. Внутри: необязательный комментарий `<!-- ص: N -->` (бледный,
  только в оригинале), структурные заголовки `#`/`##`/`###` (переводимые).
  Редактируемый (правка текста и границ абзацев), сохраняется.
- `translation/` — основной русский перевод, один файл на блок, **единственный
  итоговый артефакт**. Имя = метка: `390-393.md`. Заголовок — во фронтматтере.
  Заготовка:
  ```
  ---
  title: Виды поклонения сердца
  status: todo
  glossary_checked: false
  ---
  ```
- `variants/` — справочные варианты (другие ИИ, ручные правки). На блок может быть
  несколько, поэтому имя = метка + ярлык-источник: `390-393 gemini.md`,
  `Глава 1 мой.md`. Ярлык — свободный текст.

**Спаривание.** `source/<метка>.md` ↔ `translation/<метка>.md` — по совпадению
имени (метки). Вариант относится к блоку, чья метка является самым длинным
префиксом имени файла варианта; остаток (после пробела) = ярлык. (Метка может
содержать пробелы — поэтому именно префикс-совпадение с известными метками, а не
разбор по первому пробелу.)

**Сортировка** списка блоков — по метке по алфавиту (порядок задаётся именем;
менять порядок = менять метку).

Папка определяет роль файла — `source/`, `translation/`, `variants/` не смешивать.

### Правила фронтматтера

- Плоский YAML (`key: value`). При сохранении **сохранять все ключи и их
  порядок**; менять только нужный ключ (`title`, `status`, `glossary_checked`,
  `order`). Неизвестные ключи не выкидывать. Ключ `pages` (если есть в старых
  файлах) — сохранять, но он больше не идентификатор.
- `order` (необязательный, число) — ручной порядок раздела в списке. Появляется
  только у переставленных кнопками ▲▼ блоков; сортировка = по `order` (у кого
  есть), иначе по метке (алфавит). Перестановка пишет `order` только в один файл
  (середина между соседями), чтобы не плодить коммиты в GitHub-режиме.
- Если ключа нет — добавить в конец.
- `title` — человекочитаемый заголовок блока, показывается в панели; правка
  заголовка меняет только это поле, **без переименования файлов**.
- Тело файла нормализуем: срезаем пустые строки в начале/конце; между закрывающим
  `---` и телом — одна пустая строка.
- Оригинал (`source/`) фронтматтера не имеет.

## Окна = слоты

- Слот имеет **роль**: `source` | `translation` | `variant`. У `variant` есть
  ещё **ярлык** (= имя источника/модели).
- Слотов произвольное число; набор слотов — настройка раскладки (не привязана к
  конкретному блоку). При переходе между блоками каждый слот **сам переразрешает**
  файл текущего диапазона: слот `variant:gemini` показывает
  `variants/<диапазон> gemini.md` для активного блока.
- `source` — RTL, редактируемый; заголовки страниц и строка-комментарий рисуются
  бледно/мелко. `translation` и `variant` — LTR, редактируемые. Итогом считается
  только `translation`. У каждого окна — **одинаковая** кнопка «Сохранить» в его
  шапке, пишет только в файл этого окна; в шапке блока — «Сохранить всё».
  Несохранённое помечается фиолетовым (`--unsaved`), не цветом статуса.
- Если файла для слота в текущем блоке нет — пустое поле + «Сохранить» у варианта
  создаёт `variants/<диапазон> <ярлык>.md`. Деградировать мягко, не падать.
- Роль `footnotes` — окно сносок: показывает определения `[^id]:` того окна,
  где последним был фокус (оригинал/перевод/вариант), редактируемо. Своего файла
  нет: правки переписывают хвост определений в ведомом окне, сохранение — файл
  ведомого окна. Курсор на ссылке `[^id]` в тексте прокручивает окно сносок к её
  определению. В черновиках/dirty не участвует (учитывается ведомое окно).

## Поведение (перенесено из V1, не ломать)

- Список блоков: значок статуса (todo / draft / review / verified), диапазон,
  заголовок; сортировка по первому числу диапазона; фильтр по тексту.
- Статусы: `todo → draft → review → verified`. Смена статуса пишет во фронтматтер
  и сразу сохраняет файл (вместе с текущим телом).
- Маркеры `==?...==` (открытые вопросы) — подсветка жёлтым прямо при наборе.
  Реализация: текстовое поле + слой-подложка (родное поведение курсора/undo
  сохраняется). Регэксп живого маркера: `/==\?[\s\S]*?==/g`.
- **Запрет verified при живом `==?...==`**: если в `translation` есть маркер —
  предупредить и не менять статус.
- Отслеживание несохранённого, предупреждение при уходе/переключении блока,
  сохранение по `Ctrl/Cmd+S`.
- Поиск/замена (`Ctrl+F`): работает в окне, где последним был курсор; подсветка
  совпадений — через ту же подложку; замена через `execCommand("insertText")`,
  чтобы не ломать родную отмену.
- Панель вставки разметки (Ж/К/Ч/Сноска/==?==, `Ctrl+B/I/U`): действует на окно
  с курсором; «Сноска» сама нумерует `[^N]` и кладёт заготовку определения в
  конец текста. Подчёркивание — `<u>`, другую HTML-разметку не плодить.
- **Никогда не терять фронтматтер и чужой текст** при записи. Файлы читать свежими
  при выборе блока (их может менять git или другой инструмент).
- Кодировка — UTF-8 (арабский + русский).

## API-режим (опционально, фаза 2 — только по отдельному брифу)

- Назначение: окно-вариант может само запросить перевод оригинала у выбранной
  модели и положить ответ. Результат сохраняется **тем же** механизмом «Сохранить
  как вариант» → обычный файл в `variants/`. Хранилище не меняется.
- Провайдер по умолчанию — **OpenRouter** (один ключ, один OpenAI-совместимый
  эндпоинт, выбор модели = строка `model`). Не плодить отдельные интеграции под
  каждого провайдера без необходимости.
- **Ключ API**: у каждого пользователя свой. Хранить в `localStorage` его
  браузера, вводится один раз в настройках приложения. НИКОГДА не писать ключ в
  файлы репозитория и не коммитить — репозиторий перевода общий и раздаётся
  другим. Никакой телеметрии и внешних отправок, кроме самого вызова модели.
- При запуске с HTTPS-хостинга (Netlify) вызовы API работают без локального
  сервера — защищённый источник снимает проблему CORS. Локальный сервер нужен,
  только если открываешь как `file://` (там origin «null»).
- Обрабатывать ошибки сети/лимитов мягко (сообщение в интерфейсе, не падение).

## Хранилище за тонким слоем (фаза 3 — реализовано)

Весь доступ к файлам идёт через адаптер `state.fs` с контрактом:
`list(dir)` / `read(handle)` / `write(handle, text)` / `getFile(dir, name,
create)` / `remove(dir, name)` / `ensureWrite()`. «handle» — непрозрачный токен
своего адаптера; UI-код не знает, что внутри. Адаптера два:

- **Локальная папка** (File System Access API): handle = FileSystemFileHandle.
- **GitHub** (Contents API): handle = `{path, sha}`; сохранение = коммит в
  выбранную ветку. `sha` обновляется при каждом чтении/записи; запись со
  старым `sha` (файл изменили параллельно) даёт понятную ошибку конфликта, а
  не молчаливое затирание. Токен (fine-grained, Contents R/W на один
  репозиторий) — только в `localStorage`, как ключ OpenRouter.

Новый код не должен обращаться к File System Access или fetch GitHub напрямую —
только через `state.fs` (плюс `readFile`/`writeFile`/`ensureWritable`).
`LAST_MODE_KEY` помнит, какое хранилище переоткрывать при запуске.

## Соглашения по коду

- Строки интерфейса — на русском. Идентификаторы — на английском; комментарии
  можно на русском.
- Маленькие функции, понятные имена, без «магии». Без аналитики и трекинга.
- Любую новую возможность держать в рамках «один файл, без зависимостей».

## Режимы хранилища (state.mode)

Приложение работает в одном из режимов; ветвление — по `state.mode`:

- **черновик** (`null`) — при запуске без открытого хранилища: сразу пустые
  редактируемые окна. Папка/книга выбирается при первом «Сохранить». Кнопка
  «Закрыть папку» автосохраняет всё и возвращает в черновик.
- **папка** (`"folder"`) — классический набор `source/` + `translation/` +
  `variants/` (см. «Модель данных»). Локально (FSA) или GitHub.
- **книга** (`"library"`) — **формат читалки**, правка книги «на месте» без
  конвертации (см. ниже).

## Формат библиотеки (читалка) — режим «книга»

Цель: вычитка = редактор библиотеки читалки (репозиторий `chitalka-parallel`).
Открыть книгу читалки, поправить и сохранить — и ничего дополнительно делать не
нужно: файл, который пишет вычитка, читалка читает напрямую.

Структура книги (отличие от папочного режима):

```
<книга>/
  book.json            # title{lang}, languages[], rtl[], chapters[]{file,title{lang},status,level?,header?}
  <lang>/NN.md         # ar/01.md, ru/01.md, en/01.md … — по языку (любому)
  variants/<NN> <ярлык>.md   # черновики ИИ — только для вычитки, читалка игнорирует
  img/, cover.jpg
```

### Иерархия глав (level / группы-обёртки)

`chapters[]` — плоский массив, **порядок = порядок чтения**. Иерархия через поля:
- **`level` 0–3** — глубина (отступ/группировка в оглавлении читалки).
- **`header: true`** (без `file`) — запись-**обёртка** (Часть/`كتاب`), двуязычный
  `title{ar,ru}`; не открывается, линейная навигация её пропускает.
- Редактор (режим книги): ←→ уровень, ▲▼ порядок, ＋Группа, ✕/🗑 — пишут в `book.json`
  (`setBlockLevel`/`moveChapter`/`addGroupHeader`/`deleteChapter`). Содержимое глав
  (файлы, пары `sNNN`) при правке иерархии НЕ трогается.

### Иерархия в ПАПОЧНОМ режиме (статья) — sidecar `outline.json`

Главы статьи = файлы, поэтому:
- **`level`** главы-файла — в **`level:` фронтматтера перевода** (рядом с `status`/`order`).
- **Обёртки без файла** + общий **порядок** — в **`outline.json` в корне статьи**:
  `{ "outline": [ {wrapper,title{ar,ru},level} | {ref:"<метка>"} … ] }`. `ref` = метка
  файла-главы. Создаётся при первой иерархич. правке (`ensureOutline`); без него —
  старое поведение (алфавит + `order`). `scan()` мержит outline в `state.blocks`.
- Доступ к корневому файлу: localAdapter через `state.rootHandle`, github `getFile("","outline.json")`.
- **Публикация** переносит: обёртки → главы `{header,level,title}`, у файлов `level` в `book.json`.

Сопоставление с ролями вычитки: `languages[0]`→`source` (оригинал, RTL по
`book.rtl`, в нём маркеры страниц), `languages[1]`→`translation`, `variants/`→
варианты. Блоки = главы из `book.json.chapters` (порядок сохраняем). **`title`
и `status` живут в `book.json`**, не во фронтматтере; язык-файлы — сырой текст.
RTL — из `book.rtl`, арабский не захардкожен.

### Сектора и парность по ID (sNNN)

В формате читалки сектора **явные**: якорь `<!-- s005 -->` (под-часть
`<!-- s005a -->`) задаёт ID следующему абзацу. Парность между языками — **по
ID** (`secKey = id ?? idx`), а не по порядку. Это даёт:

- **гибридную книгу** (моноязычный «хребет» + вставные параллельные цитаты):
  сектор только в `ru/` = основной текст (во всю ширину), сектор и в `ar/`, и в
  `ru/` с одним ID = цитата (параллельно), сектор только в `ar/` = оригинал,
  перевод которого ещё не готов;
- **недопереведённые книги** можно публиковать сейчас (часть глав без перевода).

**Деление раздела** (`splitBlock`/`splitKeyAtCaret`/`splitTextAtPoint`): точка =
блок при курсоре активного окна. Анкорный сектор → ВСЕ окна режутся по
**совпадающему id** (`<!-- sN -->`, якорь уходит со своим сектором во 2-ю часть);
**заголовок `## `** — тоже валидная точка; иначе — по порядковому сектору. Новый
раздел встаёт сразу после исходного (вставка `ref` в `outline.json`) и наследует
его `level`.

Без якорей (папочный режим, старые файлы) — старое поведение «по порядку»
(обратная совместимость). Кнопка **«⌗ Якоря»** (только в режиме книги)
проставляет `<!-- sNNN -->` абзацам без якоря, продолжая нумерацию от максимума;
существующие ID не трогает (чтобы парность не плыла). Для пары «цитата» ID в
`ar/` и `ru/` должны совпадать — якорь копируется вручную.

**Якорь — всегда отдельной строкой с нулевой колонки**, НЕ внутри `>`-цитаты или
callout’а. `SECTOR_ANCHOR_RE` требует начала строки (`^<!--`): строка вида
`> <!-- s2 -->` под него не подходит → ID не присваивается (парность плывёт), а
сам якорь вылезает видимым текстом в рендере (он попадает в сектор, а не в
comment-блок). Чтобы выноска/цитата стала сектором `sN`, ставь `<!-- sN -->`
строкой выше блока (пустая строка между ними допустима — `pendingId` её переживает).

`<!-- note -->`-регионы — личные редакторские заметки автора; читалка их при
чтении прячет (не публикуются).

## Форматирование (общий словарь с читалкой)

Панель разметки — markdown-редактор: `**жирный**`, `*курсив*`,
`<u>подчёркнутый</u>`, заголовок `## `, цитата `> `, списки `- `/`1. `,
callout’ы Obsidian `> [!тип] заголовок` (тип произвольный; `+`/`-` после типа —
сворачиваемый: `[!тип]+` раскрыт, `[!тип]-` свёрнут, рендер на `<details>`),
сноски `[^N]`, открытый вопрос `==?…==`, маркер страницы, заметка. Тот же словарь
рендерит парсер читалки (`parser.js`).

## Миграция старых репозиториев в формат книги

Старый репозиторий перевода (`source/` + `translation/` + `variants/`, метка =
имя файла, заголовок/статус во фронтматтере) → книга читалки
(`books/<id>/<lang>/…` + `book.json`, sNNN, метаданные в JSON). Перенос делает
конвейер `tools/import-tam.js` читалки (образец): свёртка заголовков в
`s00Na/b`, `[^id]`→`[^N]`+`fnN`, `<!-- ص: N -->`→`<!-- pN -->`, раз-линковка
`[[вики]]`, фронтматтер→`book.json`, выброс арабских строк из ru-стороны.

**Реализовано в вычитке** — кнопка **«Опубликовать в библиотеку»** (папочный
режим): порт этой логики (`pubParse`/`pubConvert`). Выбрать папку `books/`
читалки → **форма** (id, название, языки, **раздел** = `<select>` узла
`taxonomy.json`, пишет `category=id-листа`; автор, аннотация, теги, **мазхаб**,
**жанр**, **статус** (бейдж доверия), обложка-файл, чекбокс **«только verified»**) → запись
`books/<id>/<lang>/NN.md` + `book.json` + реестр `index.json` (+ `cover.<ext>` если
выбрана обложка). `madhhab`/`genre` — фасеты каталога читалки; «только verified»
публикует лишь готовые разделы (`status === "verified"`). Оригинал пишется только при
посекторном паритете (иначе глава ru-only). Метаданные раскиданы по двум файлам:
`index.json`-запись (`category[]`, `cover`, `tags[]`, `authors[]`, `langs`) и
`book.json` (`author{lang}`, `description`). **Повторная публикация — merge**:
наследуем прежние поля обоих файлов, переписываем только производные от статьи
(главы/языки/заголовок), чтобы не терять вручную проставленные раздел/обложку/
аннотацию (`runPublish`/`pubReadJSON`/`pubPrefillById`). До миграции старые
репозитории открываются в папочном режиме как раньше; node-скрипт не нужен.

**Деплой читалки:** публикация пишет только в **локальную** папку `books/` клона
читалки (через File System Access, отдельный `showDirectoryPicker`, не `state.fs`).
Онлайн — отдельный шаг: `git push` репозитория читалки (`chitalka-parallel`,
ветка `master`) → Netlify сам пересобирает из GitHub. Кнопка ничего не пушит.

См. `CHANGELOG.md` — что и когда менялось.

## Чек-лист перед коммитом

- [ ] Один файл, без внешних зависимостей и без шага build.
- [ ] Открывается и работает как `file://` (кроме API-режима).
- [ ] Запись перевода сохраняет весь фронтматтер и не портит тело.
- [ ] `verified` блокируется при живом `==?...==`.
- [ ] Спаривание `source`/`translation`/`variants` по ведущему диапазону не сломано.
- [ ] Отсутствующие файлы/папки деградируют мягко, без падения.
- [ ] Секреты (ключи API) не попали в репозиторий.
