# Приложение — один файл vychitka.html; корень сайта отдаёт его же
# (rewrite со статусом 200: адрес в строке браузера остаётся коротким)
[[redirects]]
  from = "/"
  to = "/vychitka.html"
  status = 200
